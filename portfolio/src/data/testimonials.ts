import type { Testimonial } from "./types";

// Empty by default — add real testimonials here as you collect them.
// The section that renders these should be hidden automatically
// when this array is empty (see components/Testimonials.tsx).
export const testimonials: Testimonial[] = [];
