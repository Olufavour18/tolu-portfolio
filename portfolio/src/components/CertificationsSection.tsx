/**
 * Certifications no longer appear on the home page.
 * Full content lives on /certifications via CertificationsPage.
 * This file is kept empty-export so any stale imports fail loudly if reintroduced.
 */
export default function CertificationsSection() {
  return null;
}
